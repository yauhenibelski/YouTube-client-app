import { SortContentPipe } from './sort-content.pipe';

describe('SortContentPipe', () => {
    it('create an instance', () => {
        const pipe = new SortContentPipe();
        expect(pipe).toBeTruthy();
    });
});
